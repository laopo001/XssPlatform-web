import React from 'react';
import { Button } from 'antd';
const Example = (props) => {
  return (
    <div>
      <Button>13123npm</Button>
    </div>
  );
};

Example.propTypes = {
};

export default Example;